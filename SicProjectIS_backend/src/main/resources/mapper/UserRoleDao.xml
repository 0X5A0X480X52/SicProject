<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amatrix.sicprojectis_backend.dao.UserRoleDao">
    <resultMap id="BaseResultMap" type="com.amatrix.sicprojectis_backend.entity.UserRole">
        <id column="user_role_id" property="userRoleId" jdbcType="BIGINT" />
        <result column="user_id" property="userId" jdbcType="BIGINT" />
        <result column="role_id" property="roleId" jdbcType="BIGINT" />
        <result column="assigned_at" property="assignedAt" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="BaseColumnList">`user_role_id`, `user_id`, `role_id`, `assigned_at`</sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList" /> FROM `user_role` WHERE `user_role_id` = #{userRoleId, jdbcType=BIGINT}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList" /> FROM `user_role`
    </select>

    <insert id="insert" parameterType="com.amatrix.sicprojectis_backend.entity.UserRole" useGeneratedKeys="true" keyProperty="userRoleId" keyColumn="user_role_id">
        INSERT INTO `user_role` (
            `user_id`, `role_id`
        ) VALUES (
            #{userId, jdbcType=BIGINT}, #{roleId, jdbcType=BIGINT}
        )
    </insert>

    <update id="updateById" parameterType="com.amatrix.sicprojectis_backend.entity.UserRole">
        UPDATE `user_role`
        SET
            `user_id` = #{userId, jdbcType=BIGINT},
            `role_id` = #{roleId, jdbcType=BIGINT}
        WHERE `user_role_id` = #{userRoleId, jdbcType=BIGINT}
    </update>

    <delete id="deleteById">
        DELETE FROM `user_role` WHERE `user_role_id` = #{userRoleId, jdbcType=BIGINT}
    </delete>
</mapper>
