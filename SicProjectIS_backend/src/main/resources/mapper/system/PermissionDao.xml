<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amatrix.sicprojectis_backend.system.dao.PermissionDao">
    <resultMap id="BaseResultMap" type="com.amatrix.sicprojectis_backend.system.entity.Permission">
        <id column="permission_id" property="permissionId" jdbcType="BIGINT" />
        <result column="permission_code" property="permissionCode" jdbcType="VARCHAR" />
        <result column="permission_name" property="permissionName" jdbcType="VARCHAR" />
        <result column="permission_type" property="permissionType" jdbcType="VARCHAR" />
        <result column="permission_desc" property="permissionDesc" jdbcType="LONGVARCHAR" />
    </resultMap>

    <sql id="BaseColumnList">`permission_id`, `permission_code`, `permission_name`, `permission_type`, `permission_desc`</sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList" /> FROM `permission` WHERE `permission_id` = #{permissionId, jdbcType=BIGINT}
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList" /> FROM `permission` WHERE `permission_code` = #{permissionCode, jdbcType=VARCHAR}
    </select>

    <select id="selectPermissionCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT p.`permission_code`
        FROM `permission` p
        INNER JOIN `role_permission` rp ON rp.`permission_id` = p.`permission_id`
        INNER JOIN `user_role` ur ON ur.`role_id` = rp.`role_id`
        WHERE ur.`user_id` = #{userId, jdbcType=BIGINT}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList" /> FROM `permission`
    </select>

    <insert id="insert" parameterType="com.amatrix.sicprojectis_backend.system.entity.Permission" useGeneratedKeys="true" keyProperty="permissionId" keyColumn="permission_id">
        INSERT INTO `permission` (
            `permission_code`, `permission_name`, `permission_type`, `permission_desc`
        ) VALUES (
            #{permissionCode, jdbcType=VARCHAR}, #{permissionName, jdbcType=VARCHAR}, #{permissionType, jdbcType=VARCHAR}, #{permissionDesc, jdbcType=LONGVARCHAR}
        )
    </insert>

    <update id="updateById" parameterType="com.amatrix.sicprojectis_backend.system.entity.Permission">
        UPDATE `permission`
        SET
            `permission_code` = #{permissionCode, jdbcType=VARCHAR},
            `permission_name` = #{permissionName, jdbcType=VARCHAR},
            `permission_type` = #{permissionType, jdbcType=VARCHAR},
            `permission_desc` = #{permissionDesc, jdbcType=LONGVARCHAR}
        WHERE `permission_id` = #{permissionId, jdbcType=BIGINT}
    </update>

    <delete id="deleteById">
        DELETE FROM `permission` WHERE `permission_id` = #{permissionId, jdbcType=BIGINT}
    </delete>
</mapper>
